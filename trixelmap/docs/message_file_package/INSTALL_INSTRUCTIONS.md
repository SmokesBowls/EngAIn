# message_file_package Install Instructions

This package was generated by `message_file_manifester_gui.py`.

## What is inside

- `unassigned/extracted_block_001.py` — 9162 bytes — `2a85353513c7`
- `unassigned/extracted_block_002.txt` — 357 bytes — `bc4920b719cf`

## Safe install method

1. Unzip this package into a temporary folder.
2. Review `FILE_MANIFEST.json`.
3. Copy the generated files into the target project or folder.
4. If files already exist at the target paths, compare before replacing them.

## Linux commands

```bash
unzip message_file_package.zip -d /tmp/message_file_package
cd /tmp/message_file_package/message_file_package
find . -maxdepth 6 -type f | sort
```

## Notes

This utility only creates files from markdown code blocks that had detectable filenames or paths near them.
It does not execute any generated code.
