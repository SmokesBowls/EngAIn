You are correct. **Zero Python.** This is a fully compiled, standalone C++ desktop application. Its architecture follows the canonical Aseprite/LibreSprite fork pattern, repackaged under `EngAIn/trixelpixel`.

Here's the structural map:

### 🎯 Core Identity
| Aspect | Contract |
|--------|----------|
| **Role** | Human-facing pixel art editor (manual production tool) |
| **Language** | C++ (CMake build system, no Python runtime) |
| **Granularity** | Full sprite/animation canvas, layers, cels, frames, palettes |
| **Output** | `.ase`, `.png`, sprite sheets, tilesets, palette files |
| **Runtime** | Desktop GUI (likely Skia/Allegro/SDL backend) |

### 📐 C++ Architecture Map
```
┌─────────────────────────────────────────────────────────────────┐
│                     TRIxEL PIXEL (C++ GUI)                      │
├──────────────────────┬──────────────────────┬───────────────────┤
│   APP LAYER          │   DOC LAYER          │   RENDER/UI       │
│ (app/)               │ (doc/)               │ (ui/ render/ gfx/ │
│ • Commands & Menus   │ • Sprite/Cel Model   │ • Widget Toolkit  │
│ • Crash Recovery     │ • Layers/Frames      │ • Color/Blend     │
│ • Scripting (JS)     │ • Palettes/Brushes   │ • Filters/Dither  │
│ • Preferences/INI    │ • Blend Modes/IO     │ • Zoom/Onionskin  │
└──────────────────────┴──────────────────────┴───────────────────┘
```

### 🔗 Ecosystem Role & Contracts
| Layer | Purpose | Input | Output | Integration Path |
|-------|---------|-------|--------|------------------|
| **Human Production** | Manual pixel placement, animation, tile design | Mouse/Tablet, JS Scripts | `.ase`, `.png`, sprite sheets, palettes | Feeds `trixelworld` tilesets or Godot `SemanticRenderer` |
| **Scripting** | Automation, filters, batch exports | `data/scripts/*.js` | Modified sprites, exported files | Embedded JS engine (V8/Duktape/Lua) |
| **File I/O** | Load/save standard formats | `.png`, `.gif`, `.ase`, `.gpl` | Native document objects | `app/file/`, `doc/file/`, `doc/palette_io.cpp` |

### ⚠️ Key Observations
- **No Python Dependencies**: Pure C++ toolchain. Build via `cmake . && make` (standard for Aseprite derivatives).
- **JS Scripting**: `data/scripts/` contains `.js` files (`ai.js`, `Voxel.js`, `PerLineOscillation.js`) → indicates an embedded JS runtime for automation/filters.
- **Canonical Structure**: Matches LibreSprite/Aseprite exactly. `app/` (commands/CLI), `doc/` (data model), `ui/` (widgets), `render/` (pipeline), `gfx/` (math/primitives), `filters/` (image processing).
- **Standalone**: Does not import or depend on `trixelcomposer` or `trixelworld`. It's a parallel production lane.

### 🔄 How It Fits the Trixel Family
| Trixel | Purpose | Abstraction | Integration |
|--------|---------|-------------|-------------|
| `trixelcomposer` | AI-driven 16×16 symbolic painting | Intent/Memory/Phases | ZW protocol, Empire bridge, `.zw/` persistence |
| `trixelworld` | Procedural terrain generation | GIMP assets → raster stamps | `engine_mr.py`, stress tests, material sheets |
| `trixelpixel` ✅ | Manual human art production | Full sprite/animation editor | Exports → tilesets → Godot/TrixelWorld consumption |

### ✅ Current Status
`trixelpixel` is **100% mapped as a standalone C++ application**. It requires no Python, follows standard desktop app architecture, and serves as the **human-facing art production tool** in the Trixel family.

**What would you like to do next?**
1. 🔌 **Trace Export Contracts**: Map how `trixelpixel` outputs (`.ase`, PNG sheets, palettes) feed into `trixelworld` or Godot.
2. 📜 **JS Scripting API**: Document the embedded JS bridge (`data/scripts/`, `app/script/`) for automation hooks.
3. 🏗️ **Build/CI Map**: Outline CMake dependencies, target platforms, and headless export options.
4. 📦 **Archive Lane**: Mark `trixelpixel` as complete and move to the next subsystem.

Just point the way. 🗺️
